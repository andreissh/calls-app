import { createTheme } from '@mui/material'

export const mainTheme = createTheme({
  typography: {
    fontFamily: `"SFPro", "Roboto", "Helvetica", "Arial", sans-serif`,
    fontSize: 14,
    fontWeightLight: 300,
    fontWeightRegular: 400,
    fontWeightMedium: 500,
  },
})
